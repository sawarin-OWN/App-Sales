# 🔧 แก้ไขปัญหา Domain หายไปใน Vercel

## 🎯 ปัญหา: `sales-report-theta.vercel.app` หายไป

### สาเหตุที่เป็นไปได้:
1. **Project name ถูกเปลี่ยน** - Vercel สร้าง domain จากชื่อโปรเจค
2. **Production deployment ถูกเปลี่ยน** - Domain จะเปลี่ยนตาม deployment ที่เป็น production
3. **Domain ถูกลบหรือเปลี่ยน** - อาจถูกตั้งค่าใหม่

---

## ✅ วิธีแก้ไข

### **วิธีที่ 1: ตั้งค่า Production Domain (แนะนำ)** ⭐

#### ขั้นตอน:

1. **ไปที่ Vercel Dashboard:**
   - https://vercel.com/dashboard
   - เลือกโปรเจค `sales-report`

2. **ไปที่ Settings → Domains:**
   - คลิกแท็บ "Domains" ในเมนูซ้าย
   - หรือไปที่ Project Settings → Domains

3. **เพิ่ม Production Domain:**
   - คลิก "Add Domain"
   - ใส่ domain: `sales-report-theta.vercel.app`
   - หรือ domain อื่นที่ต้องการ

4. **ตั้งค่า Production Deployment:**
   - ไปที่ Deployments
   - เลือก deployment ที่ต้องการเป็น production
   - คลิก "..." (เมนู) → "Promote to Production"
   - หรือคลิก "Promote to Production" โดยตรง

---

### **วิธีที่ 2: เปลี่ยน Project Name กลับ**

#### ขั้นตอน:

1. **ไปที่ Vercel Dashboard:**
   - https://vercel.com/dashboard
   - เลือกโปรเจค `sales-report`

2. **ไปที่ Settings → General:**
   - คลิกแท็บ "General" ในเมนูซ้าย

3. **เปลี่ยน Project Name:**
   - หา "Project Name"
   - เปลี่ยนเป็น: `sales-report-theta`
   - คลิก "Save"

4. **ตรวจสอบ Domain:**
   - Vercel จะสร้าง domain ใหม่: `sales-report-theta.vercel.app`
   - อาจต้องรอสักครู่เพื่อให้ domain ทำงาน

---

### **วิธีที่ 3: ใช้ Custom Domain (ถ้ามี)**

#### ขั้นตอน:

1. **ไปที่ Settings → Domains**

2. **เพิ่ม Custom Domain:**
   - คลิก "Add Domain"
   - ใส่ domain ที่ต้องการ (เช่น `sales-report.yourdomain.com`)

3. **ตั้งค่า DNS:**
   - ตามคำแนะนำใน Vercel Dashboard
   - เพิ่ม CNAME record ตามที่ Vercel แนะนำ

---

## 🔍 ตรวจสอบ Domain ปัจจุบัน

### วิธีตรวจสอบ:

1. **ไปที่ Vercel Dashboard → Project → Deployments**

2. **ดูที่ Deployment Details:**
   - ดูส่วน "Domains"
   - จะแสดง domains ทั้งหมดที่ใช้งานได้

3. **ตรวจสอบ Production Domain:**
   - Production deployment จะมี domain แบบ `project-name.vercel.app`
   - Preview deployments จะมี domain แบบ `project-name-xxx-username.vercel.app`

---

## 📝 หมายเหตุ

### Domain Format ใน Vercel:
- **Production**: `project-name.vercel.app`
- **Preview**: `project-name-xxx-username.vercel.app`

### ถ้า Project Name เป็น `sales-report`:
- Production domain จะเป็น: `sales-report.vercel.app`
- ไม่ใช่ `sales-report-theta.vercel.app`

### วิธีได้ Domain `sales-report-theta.vercel.app`:
1. เปลี่ยน Project Name เป็น `sales-report-theta`
2. หรือเพิ่มเป็น Custom Domain
3. หรือใช้ Production Domain ที่ Vercel สร้างให้

---

## 🎯 แนะนำ

### วิธีที่ดีที่สุด:
1. **ใช้ Production Domain ที่ Vercel สร้างให้** (เช่น `sales-report.vercel.app`)
2. **หรือเปลี่ยน Project Name** เป็น `sales-report-theta` เพื่อให้ได้ domain ที่ต้องการ
3. **หรือเพิ่ม Custom Domain** ถ้ามี domain ของตัวเอง

---

## ⚠️ ข้อควรระวัง

- **Domain จะเปลี่ยน** เมื่อเปลี่ยน Project Name
- **ต้องอัปเดต URL** ใน Google Apps Script (ถ้ามีการอ้างอิง)
- **ต้องอัปเดต PWA** บนมือถือ (ลบและเพิ่มใหม่)

---

## 🔗 ลิงก์ที่เกี่ยวข้อง

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Domains Documentation](https://vercel.com/docs/concepts/projects/domains)


