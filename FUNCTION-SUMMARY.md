# 📋 สรุปฟังก์ชันการทำงานของแอป KebYod App

## 🎯 ภาพรวมระบบ

แอปพลิเคชันสำหรับบันทึกและจัดการยอดขาย รายงานค่าใช้จ่าย และการนำฝากเงินสด พัฒนาด้วย **React** (Frontend) และ **Google Apps Script** (Backend) โดยใช้ **Google Sheets** เป็นฐานข้อมูล

---

## 🔐 1. ระบบ Authentication (การยืนยันตัวตน)

### หน้าที่:
- ตรวจสอบสิทธิ์การเข้าสู่ระบบด้วย Email และ Password
- จัดการ Session และ Token
- ป้องกันการเข้าถึงหน้าสำคัญโดยไม่ล็อกอิน

### ไฟล์ที่เกี่ยวข้อง:
- `src/components/Login.js` - หน้าจอ Login
- `src/context/AuthContext.js` - จัดการ Authentication State
- `backend/Code.js` → `loginUser()` - ตรวจสอบข้อมูลจาก Google Sheets

### ฟังก์ชันหลัก:
```javascript
loginUser(email, password)
// ตรวจสอบ Email/Password จาก User Sheet
// ส่งกลับข้อมูล User (BranchCode, BranchName, Token)
```

---

## 📊 2. Dashboard (ภาพรวม)

### หน้าที่:
- แสดงสรุปยอดขายรวม, กำไรสุทธิ, ค่าใช้จ่ายรวม
- แสดงกราฟยอดขายรายวัน (Line Chart)
- แสดงกราฟยอดขายแยกตามช่องทาง (Doughnut Chart)
- กรองข้อมูลตามช่วงวันที่ (เริ่มต้น-สิ้นสุด)

### ไฟล์ที่เกี่ยวข้อง:
- `src/components/Dashboard.js` - หน้าจอ Dashboard
- `backend/Code.js` → `getDashboardData()` - ดึงข้อมูลสรุป

### ฟังก์ชันหลัก:
```javascript
getDashboardData(branchCode, startDate, endDate)
// คำนวณยอดขายรวม, กำไรสุทธิ, ค่าใช้จ่าย
// สร้างข้อมูลกราฟ Line และ Pie
// ส่งกลับข้อมูลสรุปและกราฟ
```

### ข้อมูลที่แสดง:
- **ยอดขายรวม** (Total Sales)
- **กำไรสุทธิ** (Net Profit = ยอดขาย - ค่าใช้จ่าย)
- **ค่าใช้จ่ายรวม** (Total Expenses)
- **จำนวนรายการ** (Total Records)
- **กราฟยอดขายรายวัน** (Line Chart)
- **กราฟยอดขายแยกตามช่องทาง** (Doughnut Chart)

---

## 💰 3. Sales (ปิดยอดสิ้นวัน)

### หน้าที่:
- บันทึกยอดขายแยกตามช่องทาง (เงินสด, โอน, Grab, Lineman, Shopee, Robinhood, บัตรเครดิต, คนละครึ่ง, อื่นๆ)
- บันทึกเงินสดเริ่มต้น, เงินสดนับได้, มูลค่าของเสีย
- คำนวณยอดรวม, เงินคงเหลือ, เงินขาด/เกิน, เงินนำฝาก
- แสดงค่าใช้จ่ายเงินสดของวันที่เลือก
- แนบรูปภาพหลักฐานการปิดยอด
- แก้ไขข้อมูลยอดขายที่บันทึกไว้แล้ว
- แสดงสถานะวันที่ที่มีข้อมูลแล้ว

### ไฟล์ที่เกี่ยวข้อง:
- `src/components/Sales.js` - หน้าจอปิดยอด
- `backend/Code.js` → `saveSalesData()`, `getSalesData()`, `updateSalesData()`, `getSalesDates()`
- `backend/Code.js` → `getCashExpensesForDate()` - ดึงค่าใช้จ่ายเงินสด

### ฟังก์ชันหลัก:

#### บันทึกข้อมูลใหม่:
```javascript
saveSalesData(data)
// รับข้อมูล: date, cash, transfer, grab, lineman, shopee, 
//            robinhood, creditCard, halfHalf, other,
//            startingCash, cashCounted, wasteAmount, notes,
//            receiptImageBase64, receiptFileName
// คำนวณ: Total, CashBalance, CashDiff, DepositAmount
// อัพโหลดรูปภาพไป Google Drive (ถ้ามี)
// บันทึกลง Sales Sheet
```

#### ดึงข้อมูลที่มีอยู่:
```javascript
getSalesData(branchCode, date)
// ค้นหาข้อมูลยอดขายตาม BranchCode และ Date
// ส่งกลับข้อมูลล่าสุด (ถ้ามีหลายรายการ)
// รวมถึง URL รูปภาพจาก Google Drive
```

#### อัพเดทข้อมูล:
```javascript
updateSalesData(data)
// อัพเดทข้อมูลยอดขายตาม rowId
// อัพโหลดรูปภาพใหม่ถ้ามี (และลบรูปเก่า)
// อัพเดท UpdatedAt timestamp
```

#### ดึงรายการวันที่ที่มีข้อมูล:
```javascript
getSalesDates(branchCode)
// ดึงรายการวันที่ทั้งหมดที่มีข้อมูลยอดขาย
// ใช้แสดงสถานะในปฏิทิน
```

### ฟีเจอร์พิเศษ:
- ✅ **Auto-clear ข้อมูล** เมื่อเปลี่ยนวันที่
- ✅ **แสดงสถานะวันที่** ที่มีข้อมูลแล้ว (สีเขียว)
- ✅ **Toast notification** เมื่อกำลังโหลดข้อมูล (ไม่บัง input)
- ✅ **คำนวณอัตโนมัติ** ยอดรวม, เงินคงเหลือ, เงินขาด/เกิน
- ✅ **แสดงค่าใช้จ่ายเงินสด** ของวันที่เลือก
- ✅ **อัพโหลดรูปภาพ** ไป Google Drive และเก็บ URL

---

## 💸 4. Expenses (ค่าใช้จ่าย)

### หน้าที่:
- บันทึกค่าใช้จ่าย (ประเภท: CASH_DRAWER, BANK_TRANSFER, OTHER)
- แนบรูปภาพหลักฐานค่าใช้จ่าย
- แสดงประวัติค่าใช้จ่าย
- แก้ไขข้อมูลค่าใช้จ่าย

### ไฟล์ที่เกี่ยวข้อง:
- `src/components/Expenses.js` - หน้าจอค่าใช้จ่าย
- `backend/Code.js` → `saveExpenseData()`, `getExpenseHistory()`

### ฟังก์ชันหลัก:

#### บันทึกค่าใช้จ่าย:
```javascript
saveExpenseData(data)
// รับข้อมูล: date, amount, expenseType, reason, notes,
//            receiptImageBase64, receiptFileName
// อัพโหลดรูปภาพไป Google Drive (ถ้ามี)
// บันทึกลง Expenses Sheet
```

#### ดึงประวัติค่าใช้จ่าย:
```javascript
getExpenseHistory(branchCode)
// ดึงประวัติค่าใช้จ่ายทั้งหมดของสาขา
// แสดงตามวันที่ (ล่าสุดก่อน)
// รวม URL รูปภาพจาก Google Drive
```

### ข้อมูลที่บันทึก:
- **วันที่** (Date)
- **จำนวนเงิน** (Amount)
- **ประเภท** (Type: CASH_DRAWER, BANK_TRANSFER, OTHER)
- **เหตุผล** (Reason)
- **หมายเหตุ** (Notes)
- **รูปภาพหลักฐาน** (Receipt Image URL)

---

## 🏦 5. Deposits (นำฝากเงินสด)

### หน้าที่:
- บันทึกการนำฝากเงินสด
- แนบรูปภาพสลิปการโอนเงิน
- แสดงประวัติการนำฝาก
- คำนวณยอดค้างนำฝาก (Pending Balance)

### ไฟล์ที่เกี่ยวข้อง:
- `src/components/Deposits.js` - หน้าจอนำฝาก
- `backend/Code.js` → `saveDepositData()`, `getDepositInfo()`

### ฟังก์ชันหลัก:

#### บันทึกการนำฝาก:
```javascript
saveDepositData(data)
// รับข้อมูล: date, amount, notes, slipBase64, slipFileName
// อัพโหลดรูปภาพสลิปไป Google Drive
// บันทึกลง Deposits Sheet
```

#### คำนวณยอดค้างนำฝาก:
```javascript
getDepositInfo(branchCode)
// คำนวณ: Pending Balance = Total Sales - Total Expenses - Total Deposits
// ดึงประวัติการนำฝากทั้งหมด
// ส่งกลับ pendingBalance และ history
```

### สูตรคำนวณ:
```
Pending Balance = ยอดขายรวม - ค่าใช้จ่ายรวม - เงินนำฝากรวม
```

---

## 🖼️ 6. Image Upload (การอัพโหลดรูปภาพ)

### หน้าที่:
- อัพโหลดรูปภาพหลักฐาน (ใบเสร็จ, สลิป) ไป Google Drive
- บีบอัดรูปภาพให้มีขนาดเล็ก (< 20KB)
- เก็บ URL รูปภาพใน Google Sheets แทน Base64

### ไฟล์ที่เกี่ยวข้อง:
- `src/utils/imageCompression.js` - บีบอัดรูปภาพ
- `backend/Code.js` → `uploadImageToDrive()` - อัพโหลดไป Drive

### กระบวนการ:
1. **Frontend**: ผู้ใช้เลือกไฟล์ → บีบอัดรูปภาพ → ส่ง Base64 ไป Backend
2. **Backend**: รับ Base64 → แปลงเป็น Blob → อัพโหลดไป Google Drive → เก็บ URL
3. **Database**: เก็บ URL ใน Google Sheets (ไม่เก็บ Base64)

### ฟังก์ชันหลัก:
```javascript
// Frontend
compressImage(file, maxSizeKB = 20)
// บีบอัดรูปภาพให้มีขนาด < 20KB
// ลดขนาดและคุณภาพแบบ iterative

// Backend
uploadImageToDrive(base64String, fileName, folderId)
// แปลง Base64 เป็น Blob
// อัพโหลดไป Google Drive
// ส่งกลับ File URL
```

---

## 📅 7. Date Handling (การจัดการวันที่)

### หน้าที่:
- Normalize วันที่ให้เป็นรูปแบบ `YYYY-MM-DD` เสมอ
- จัดการ Timezone differences ระหว่าง Frontend และ Backend
- ใช้ `getDisplayValues()` ใน GAS เพื่อหลีกเลี่ยงปัญหา Timezone

### ไฟล์ที่เกี่ยวข้อง:
- `src/utils/dateUtils.js` - ฟังก์ชันจัดการวันที่
- `backend/Code.js` → `normalizeDate()` - Normalize วันที่ใน GAS

### ฟังก์ชันหลัก:
```javascript
// Frontend
normalizeDate(dateInput)
// แปลงวันที่ทุกรูปแบบเป็น YYYY-MM-DD
// รองรับ: Date object, string (MM/DD/YYYY, DD/MM/YYYY, YYYY-MM-DD)

getTodayDate()
// ส่งกลับวันที่ปัจจุบันในรูปแบบ YYYY-MM-DD

// Backend
normalizeDate(dateInput)
// ใช้ Utilities.formatDate() กับ Session.getScriptTimeZone()
// แปลงเป็น YYYY-MM-DD
```

---

## 🔄 8. API Communication (การสื่อสารระหว่าง Frontend-Backend)

### หน้าที่:
- สื่อสารระหว่าง React App และ Google Apps Script
- ใช้ `fetch` POST method ส่ง JSON
- จัดการ Error และ Loading states

### ไฟล์ที่เกี่ยวข้อง:
- `src/services/gasAPI.js` - API Service Layer
- `backend/Code.js` → `doPost()`, `doGet()` - Request Handlers

### ฟังก์ชันหลัก:

#### Frontend:
```javascript
apiCall(action, payload)
// ส่ง POST request ไป GAS Web App
// action: ชื่อฟังก์ชันที่ต้องการเรียก (เช่น 'saveSalesData')
// payload: ข้อมูลที่ต้องการส่ง
// ส่งกลับ JSON response

callGASFunction(functionName, ...args)
// Wrapper สำหรับเรียกฟังก์ชัน GAS
// แปลง arguments เป็น object และเรียก apiCall()
```

#### Backend:
```javascript
doPost(e)
// รับ POST request จาก Frontend
// Parse JSON จาก e.postData.contents
// Route ไปยังฟังก์ชันที่เหมาะสมตาม action
// ส่งกลับ JSON response

doGet(e)
// รับ GET request (สำหรับ testing)
// ส่งกลับ JSON response
```

### API Endpoints:
- `loginUser` - Login
- `getDashboardData` - ดึงข้อมูล Dashboard
- `saveSalesData` - บันทึกยอดขาย
- `getSalesData` - ดึงข้อมูลยอดขาย
- `updateSalesData` - อัพเดทยอดขาย
- `getSalesDates` - ดึงรายการวันที่ที่มีข้อมูล
- `saveExpenseData` - บันทึกค่าใช้จ่าย
- `getExpenseHistory` - ดึงประวัติค่าใช้จ่าย
- `getCashExpensesForDate` - ดึงค่าใช้จ่ายเงินสดของวันที่
- `saveDepositData` - บันทึกการนำฝาก
- `getDepositInfo` - ดึงข้อมูลการนำฝากและยอดค้าง

---

## 📱 9. Responsive Design (รองรับทุกอุปกรณ์)

### หน้าที่:
- รองรับการใช้งานบน Desktop, Tablet, Mobile
- ปรับ Layout และ Font Size ให้เหมาะสม
- รองรับการถ่ายภาพด้วยกล้องมือถือ

### ฟีเจอร์:
- ✅ **Mobile-first Design** - ออกแบบให้ใช้งานบนมือถือได้ดี
- ✅ **Touch-friendly** - ปุ่มและ input field มีขนาดเหมาะสม
- ✅ **Camera Access** - รองรับการถ่ายภาพด้วยกล้องมือถือ (`capture="environment"`)
- ✅ **Responsive Charts** - กราฟปรับขนาดตามหน้าจอ

---

## 🎨 10. UI/UX Features (ฟีเจอร์ส่วนติดต่อผู้ใช้)

### ฟีเจอร์:
- ✅ **Toast Notifications** - แจ้งเตือนแบบไม่บังหน้าจอ (ใช้ SweetAlert2)
- ✅ **Loading States** - แสดงสถานะกำลังโหลด
- ✅ **Form Validation** - ตรวจสอบข้อมูลก่อนบันทึก
- ✅ **Auto-calculate** - คำนวณอัตโนมัติเมื่อกรอกข้อมูล
- ✅ **Date Status Indicator** - แสดงสถานะวันที่ที่มีข้อมูลแล้ว
- ✅ **Image Preview** - แสดงตัวอย่างรูปภาพก่อนอัพโหลด
- ✅ **Error Handling** - แสดงข้อความ error ที่เข้าใจง่าย

---

## 📊 11. Data Structure (โครงสร้างข้อมูล)

### Google Sheets Structure:

#### 1. User Sheet
- **Column A**: Email
- **Column B**: Password (hashed)
- **Column F**: Branch Code
- **Column G**: Branch Name
- **Column H**: Token

#### 2. Sales Sheet
- **Headers**: BranchCode, Date, Cash, Transfer, Grab, Lineman, Shopee, Robinhood, CreditCard, HalfHalf, Other, Total, StartingCash, StaffDiscount, PromoDiscount, CashCounted, DepositAmount, CashBalance, CashDiff, WasteAmount, Notes, ReceiptImageUrl, ReceiptFileName, CreatedBy, CreatedAt, UpdatedAt

#### 3. Expenses Sheet
- **Headers**: BranchCode, Date, Amount, Type, Reason, Notes, ReceiptImageUrl, ReceiptFileName, CreatedBy, CreatedAt

#### 4. Deposits Sheet
- **Headers**: BranchCode, Date, Amount, SlipImageUrl, SlipFileName, Notes, CreatedBy, CreatedAt

---

## 🔒 12. Security Features (ความปลอดภัย)

### ฟีเจอร์:
- ✅ **Authentication** - ต้อง Login ก่อนใช้งาน
- ✅ **Token-based Session** - ใช้ Token สำหรับ Session
- ✅ **Branch Code Validation** - ตรวจสอบ Branch Code ทุก request
- ✅ **Date Normalization** - ป้องกันปัญหา Timezone
- ✅ **Input Validation** - ตรวจสอบข้อมูลก่อนบันทึก

---

## 🚀 13. Deployment (การ Deploy)

### Frontend (React):
- **Development**: `npm start` → `http://localhost:3000`
- **Production**: `npm run build` → Deploy ไป Netlify/Vercel/GitHub Pages

### Backend (Google Apps Script):
- **Deploy as Web App**:
  - Execute as: Me
  - Who has access: Anyone
  - Copy Web App URL → ตั้งค่าใน `src/services/gasAPI.js`

---

## 📝 สรุป

แอปนี้เป็นระบบ **KebYod App** ที่ครอบคลุม:
- ✅ การบันทึกยอดขายแยกตามช่องทาง
- ✅ การบันทึกค่าใช้จ่าย
- ✅ การบันทึกการนำฝากเงินสด
- ✅ การคำนวณยอดรวม, กำไร, เงินขาด/เกิน
- ✅ Dashboard แสดงกราฟและสรุปข้อมูล
- ✅ การอัพโหลดรูปภาพหลักฐาน
- ✅ การแก้ไขข้อมูลที่บันทึกไว้แล้ว
- ✅ Responsive Design รองรับทุกอุปกรณ์

**เทคโนโลยีหลัก:**
- Frontend: React, Tailwind CSS, Chart.js, SweetAlert2
- Backend: Google Apps Script
- Database: Google Sheets
- Storage: Google Drive (สำหรับรูปภาพ)

