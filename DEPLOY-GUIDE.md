# 🚀 คู่มือ Deploy ไป Vercel (แนะนำ)

## ⭐ วิธีที่แนะนำ: ใช้ Build Script + Vercel Dashboard

### ✅ ข้อดี:
- **อัตโนมัติ** - Build Script ทำงานเอง (ตั้งค่าไว้แล้ว)
- **ง่าย** - ไม่ต้องจำขั้นตอน
- **ปลอดภัย** - Backup ไฟล์เดิมอัตโนมัติ
- **ไม่กระทบ Development** - ใช้ไฟล์เดิมตอน `npm start`

---

## 📋 ขั้นตอนการ Deploy

### **วิธีที่ 1: Deploy ผ่าน Vercel Dashboard (แนะนำที่สุด)** ⭐

#### ขั้นตอน:

1. **Build โปรเจค (ถ้ายังไม่ได้ build):**
   ```bash
   npm run build
   ```
   - ✅ Build Script จะ copy `index.production.html` → `index.html` อัตโนมัติ
   - ✅ สร้างโฟลเดอร์ `build/` พร้อม deploy

2. **ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)**

3. **คลิก "Add New Project"**

4. **Import Git Repository:**
   - ถ้ามี Git: เชื่อมต่อ GitHub/GitLab/Bitbucket
   - ถ้าไม่มี Git: ใช้วิธีที่ 2 (Vercel CLI)

5. **ตั้งค่า Project:**
   - **Framework Preset**: `Create React App`
   - **Root Directory**: `.` (root)
   - **Build Command**: `npm run build` (ใช้ default)
   - **Output Directory**: `build` (ใช้ default)
   - **Install Command**: `npm install` (ใช้ default)

6. **Environment Variables (ถ้ามี):**
   - `REACT_APP_GAS_URL`: URL ของ Google Apps Script
   - เพิ่มใน Vercel Dashboard → Settings → Environment Variables

7. **คลิก "Deploy"**

8. **รอให้ Deploy เสร็จ** (ประมาณ 1-2 นาที)

9. **ได้ URL** เช่น: `https://sales-report-theta.vercel.app`

---

### **วิธีที่ 2: Deploy ผ่าน Vercel CLI (สำหรับไม่มี Git)**

#### ขั้นตอน:

1. **ติดตั้ง Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Login:**
   ```bash
   vercel login
   ```

3. **Deploy:**
   ```bash
   # Deploy to preview
   vercel

   # Deploy to production
   vercel --prod
   ```

4. **Vercel จะ:**
   - ✅ Detect React app อัตโนมัติ
   - ✅ รัน `npm run build` (ซึ่งจะใช้ production HTML อัตโนมัติ)
   - ✅ Deploy ไปยัง Vercel

---

## 🔍 ตรวจสอบหลัง Deploy

### 1. ตรวจสอบ Build Output:
- ไปที่ Vercel Dashboard → Deployments → เลือก deployment ล่าสุด
- ดู Build Logs ว่ามี error หรือไม่

### 2. ตรวจสอบบน Mobile:
- เปิด URL บนมือถือ
- ตรวจสอบว่า:
  - ✅ Header อยู่ใต้ status bar (ไม่ทับ)
  - ✅ หน้าจอเต็มจอ (ไม่มี address bar ใน PWA mode)
  - ✅ เมื่อกด input ไม่เลื่อนหน้าจอ

### 3. ตรวจสอบ PWA:
- เพิ่มไปหน้าแรก (Add to Home Screen)
- เปิดจาก Home Screen
- ตรวจสอบว่าแสดงเต็มจอ

---

## ⚙️ การตั้งค่า Build Script (ตั้งค่าไว้แล้ว)

### ไฟล์ที่เกี่ยวข้อง:
- `scripts/copy-production-html.js` - Script สำหรับ copy production HTML
- `package.json` - มี `prebuild` script ที่รันอัตโนมัติ

### การทำงาน:
```json
{
  "scripts": {
    "prebuild": "node scripts/copy-production-html.js",  // ← รันก่อน build
    "build": "react-scripts build"                        // ← build ตามปกติ
  }
}
```

### เมื่อรัน `npm run build`:
1. ✅ รัน `prebuild` → copy `index.production.html` → `index.html`
2. ✅ รัน `build` → build React app ด้วย production HTML
3. ✅ สร้างโฟลเดอร์ `build/` พร้อม deploy

---

## 🎯 สรุป: ควรใช้วิธีไหน?

### ✅ **แนะนำ: วิธีที่ 1 (Vercel Dashboard)**
- **ง่ายที่สุด** - ไม่ต้องติดตั้งอะไรเพิ่ม
- **อัตโนมัติ** - Build Script ทำงานเอง
- **มี UI** - ดู logs และ settings ได้ง่าย
- **เหมาะสำหรับ**: ทุกคน

### ✅ **ทางเลือก: วิธีที่ 2 (Vercel CLI)**
- **เร็ว** - Deploy จาก terminal
- **เหมาะสำหรับ**: คนที่ชอบใช้ command line

---

## 📝 หมายเหตุ

### หลัง Deploy:
1. **Clear Cache บนมือถือ** (ถ้ายังเห็นปัญหา)
2. **ลบ PWA เก่า** และเพิ่มใหม่ (iOS)
3. **Hard Refresh** (Ctrl+Shift+R)

### ถ้ามีปัญหา:
- ตรวจสอบ Build Logs ใน Vercel Dashboard
- ตรวจสอบ Console บนมือถือ (Chrome DevTools)
- ตรวจสอบว่า `index.production.html` มี safe area CSS

---

## 🎉 พร้อม Deploy!

ตอนนี้ทุกอย่างพร้อมแล้ว:
- ✅ Build Script ตั้งค่าไว้แล้ว
- ✅ Production HTML มี safe area support
- ✅ Build สำเร็จแล้ว
- ✅ พร้อม deploy ไปยัง Vercel

**แค่ไปที่ Vercel Dashboard และ Deploy!** 🚀

