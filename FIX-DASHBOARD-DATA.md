# 🔧 แก้ไขข้อมูล Dashboard ให้ตรงกับ Sheets

## 📋 ปัญหาที่พบ

1. **ข้อมูลไม่ตรงกับ Sheets**: ข้อมูลที่แสดงใน Dashboard ไม่ตรงกับข้อมูลใน Sheets
2. **วันที่ไม่ได้เรียง**: วันที่ไม่ได้เรียงจากใหม่ไปเก่า

## ✅ การแก้ไข

### 1. เปลี่ยนจาก `getDisplayValues()` เป็น `getValues()`

**ปัญหา**: 
- `getDisplayValues()` จะได้ค่าที่ format แล้ว (string) เช่น "1,064.24"
- เมื่อใช้ `parseFloat()` อาจได้ค่าผิดพลาด

**วิธีแก้**:
- ใช้ `getValues()` เพื่อให้ได้ค่าจริง (number) จาก Sheets
- ตรวจสอบว่าเป็น number หรือไม่ก่อน parse

### 2. เพิ่มการเรียงข้อมูลตามวันที่

**ปัญหา**: 
- ข้อมูลไม่ได้เรียงตามวันที่

**วิธีแก้**:
- เพิ่มการเรียงข้อมูลตามวันที่จากใหม่ไปเก่า (descending order)
- ใช้ `normalizeDate()` เพื่อเปรียบเทียบวันที่

### 3. แก้ไขการแปลงวันที่

**ปัญหา**: 
- วันที่จาก Sheets อาจเป็น Date object หรือ string

**วิธีแก้**:
- ตรวจสอบว่าเป็น Date object หรือ string
- แปลงเป็น YYYY-MM-DD format เสมอ

## 📝 ไฟล์ที่แก้ไข

- `backend/Code.js` - ฟังก์ชัน `getDashboardData`:
  - เปลี่ยนจาก `getDisplayValues()` เป็น `getValues()`
  - เพิ่มการเรียงข้อมูลตามวันที่
  - แก้ไขการแปลงวันที่และค่าตัวเลข

## 🚀 Deploy

### 1. Deploy Google Apps Script

1. เปิด Google Apps Script Editor
2. Copy โค้ดจาก `backend/Code.js` ไปวาง
3. Deploy > Manage deployments > Edit > New version > Deploy

### 2. ทดสอบ

1. เปิด Dashboard: https://sales-report-theta.vercel.app
2. Refresh หน้าเว็บ (Ctrl+F5)
3. ตรวจสอบ:
   - ข้อมูลในตารางตรงกับ Sheets
   - วันที่เรียงจากใหม่ไปเก่า

## ⚠️ หมายเหตุ

- ต้อง Deploy Google Apps Script ใหม่เพื่อให้การแก้ไขมีผล
- Refresh หน้าเว็บเพื่อล้าง cache
- ตรวจสอบ Console (F12) เพื่อดู warning หรือ error
