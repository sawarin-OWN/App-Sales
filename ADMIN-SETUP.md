# 🔐 คู่มือตั้งค่า Admin Access

## 📋 เงื่อนไขการเข้าถึงหน้า Admin

ระบบจะแสดงเมนู "หลังบ้าน" เมื่อ:
1. **Email มีคำว่า 'admin'** (ไม่สนใจตัวพิมพ์เล็ก/ใหญ่)
   - เช่น: `admin@example.com`, `administrator@example.com`, `testadmin@example.com`
2. **หรือ role === 'admin'** (ต้องเพิ่ม column Role ใน Sheet)

---

## ✅ วิธีที่ 1: ใช้ Email ที่มีคำว่า 'admin' (แนะนำ - ง่ายที่สุด)

### ขั้นตอน:

1. **เปิด Google Sheets ที่เก็บข้อมูล User**
   - Sheet ID: `1K7PN4s_SNl5kqq_uLaV54f5Lh4IUwxmj16qeh1DrG_A`
   - Sheet ชื่อ: **"User"**

2. **เพิ่ม User ใหม่หรือแก้ไข User ที่มีอยู่:**
   - **Column A (Email)**: ใส่ email ที่มีคำว่า 'admin'
     - ตัวอย่าง: `admin@saocafe.com`
     - หรือ: `administrator@saocafe.com`
     - หรือ: `superadmin@saocafe.com`
   - **Column B (Password)**: ตั้งรหัสผ่านที่ต้องการ
   - **Column C (Name)**: ใส่ชื่อผู้ใช้
   - **Column F (BranchCode)**: ใส่ BranchCode (เช่น `ADMIN` หรือ `RS000`)
   - **Column G (BranchName)**: ใส่ชื่อสาขา (เช่น `Admin` หรือ `สำนักงานใหญ่`)

3. **ตัวอย่างข้อมูลใน Sheet:**
   ```
   Email                | Password | Name      | ... | BranchCode | BranchName
   admin@saocafe.com    | admin123 | Admin     | ... | ADMIN      | สำนักงานใหญ่
   ```

4. **Login ด้วย Email และ Password ที่ตั้งไว้**

---

## ✅ วิธีที่ 2: ใช้ Role Column (ต้องปรับ Backend)

### ขั้นตอน:

1. **เพิ่ม Column Role ใน Sheet "User"**
   - เพิ่ม Column H (หรือ column ใหม่) สำหรับ Role
   - ใส่ค่า `admin` สำหรับ user ที่ต้องการให้เป็น admin

2. **ปรับ Backend Code:**
   - แก้ไข `loginUser()` function ใน `backend/Code.js`
   - เพิ่มการอ่าน Role จาก Sheet
   - ส่ง Role กลับมาใน response

3. **ตัวอย่าง:**
   ```javascript
   var role = rows[i][7] ? rows[i][7].toString().trim() : ''; // Column H
   
   return {
     status: "success",
     user: {
       email: rows[i][0],
       name: rows[i][2] || '',
       branchCode: branchCode,
       branchName: branchName || rows[i][2] || 'สาขา',
       role: role, // เพิ่ม role
       token: token
     }
   };
   ```

---

## 🎯 วิธีที่แนะนำ: ใช้ Email Pattern

**วิธีที่ง่ายที่สุดคือใช้ Email ที่มีคำว่า 'admin'**

### ตัวอย่าง Email ที่ใช้ได้:
- ✅ `admin@saocafe.com`
- ✅ `administrator@saocafe.com`
- ✅ `admin.sao@saocafe.com`
- ✅ `superadmin@saocafe.com`
- ✅ `testadmin@saocafe.com`

### ตัวอย่าง Email ที่ใช้ไม่ได้:
- ❌ `sao@saocafe.com` (ไม่มีคำว่า 'admin')
- ❌ `user@saocafe.com` (ไม่มีคำว่า 'admin')

---

## 📝 ตัวอย่างการตั้งค่าใน Sheet "User"

| Email | Password | Name | ... | BranchCode | BranchName |
|-------|----------|------|-----|------------|------------|
| admin@saocafe.com | admin123 | Admin | ... | ADMIN | สำนักงานใหญ่ |
| sao00005@saocafe.com | Sao00005 | สาขา 1 | ... | RS001 | รังสรรค์ สาขา 1 |
| sao00006@saocafe.com | Sao00006 | สาขา 2 | ... | RS002 | รังสรรค์ สาขาบางซ่อน |

---

## 🔍 ตรวจสอบว่าเป็น Admin หรือไม่

หลังจาก Login แล้ว:
1. ตรวจสอบว่าเห็นเมนู "หลังบ้าน" ในแถบเมนูด้านบน
2. ถ้าไม่เห็น แสดงว่า email ไม่มีคำว่า 'admin' หรือ role ไม่ใช่ 'admin'

---

## ⚠️ หมายเหตุ

- ระบบจะตรวจสอบ email โดยไม่สนใจตัวพิมพ์เล็ก/ใหญ่
- ถ้าต้องการเปลี่ยนเงื่อนไข แก้ไขใน `src/components/Layout.js`:
  ```javascript
  {(user?.email?.toLowerCase().includes('admin') || user?.role === 'admin') && (
  ```
