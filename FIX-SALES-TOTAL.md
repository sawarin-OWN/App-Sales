# 🔧 คู่มือแก้ไข Total ใน Sales Sheet

## 📋 วัตถุประสงค์

แก้ไข Total (Column L) ใน Sales Sheet ให้ถูกต้องโดยคำนวณใหม่จากข้อมูลจริง เนื่องจากมีการบันทึกข้อมูลโดยการกรอกลงที่ Google Sheets โดยตรง ทำให้ Total ผิดพลาด

## 🔍 ปัญหาที่พบ

- Total (Column L) ใน Sheets ไม่ตรงกับผลรวมของช่องทางการชำระเงิน
- ตัวอย่าง: วันที่ 15 ม.ค. แสดง Total = 10.00 แต่ควรเป็น 513.46 (2.00 + 4.00 + 0.00 + 507.46)

## ✅ วิธีแก้ไข

### วิธีที่ 1: ใช้ Google Apps Script Function (แนะนำ)

#### ขั้นตอนที่ 1: เปิด Google Apps Script Editor

1. เปิด Google Sheets ที่ต้องการแก้ไข
2. คลิก **Extensions** > **Apps Script**
3. Copy โค้ดจาก `backend/Code.js` ไปวาง (ถ้ายังไม่ได้ copy)

#### ขั้นตอนที่ 2: รันฟังก์ชัน

**ตัวเลือก A: แก้ไขทุกสาขา**

1. ใน Google Apps Script Editor
2. คลิกที่ฟังก์ชัน `fixSalesTotalColumn` ใน dropdown
3. คลิก **Run** (▶️)
4. Authorize permissions (ถ้ายังไม่ได้ authorize)
5. ดูผลลัพธ์ใน Logs

**ตัวเลือก B: แก้ไขเฉพาะสาขา**

1. สร้างฟังก์ชันใหม่:
```javascript
function fixSalesTotalForBranch() {
  var result = fixSalesTotalColumn('SA008'); // เปลี่ยนเป็น Branch Code ที่ต้องการ
  Logger.log(JSON.stringify(result, null, 2));
  return result;
}
```

2. คลิก **Run** > `fixSalesTotalForBranch`
3. ดูผลลัพธ์ใน Logs

### วิธีที่ 2: ใช้ API จาก Frontend

#### ขั้นตอนที่ 1: เปิด Browser Console

1. เปิด Dashboard
2. กด F12 เพื่อเปิด Developer Tools
3. ไปที่แท็บ **Console**

#### ขั้นตอนที่ 2: เรียกใช้ API

**แก้ไขทุกสาขา:**
```javascript
// เรียกใช้ API
fetch('YOUR_GAS_WEB_APP_URL', {
  method: 'POST',
  body: JSON.stringify({
    action: 'fixSalesTotalColumn',
    branchCode: null
  })
})
.then(res => res.json())
.then(data => {
  console.log('ผลลัพธ์:', data);
  alert('แก้ไขสำเร็จ: ' + data.message);
})
.catch(err => {
  console.error('เกิดข้อผิดพลาด:', err);
  alert('เกิดข้อผิดพลาด: ' + err.message);
});
```

**แก้ไขเฉพาะสาขา:**
```javascript
// แก้ไขเฉพาะสาขา SA008
fetch('YOUR_GAS_WEB_APP_URL', {
  method: 'POST',
  body: JSON.stringify({
    action: 'fixSalesTotalColumn',
    branchCode: 'SA008'
  })
})
.then(res => res.json())
.then(data => {
  console.log('ผลลัพธ์:', data);
  alert('แก้ไขสำเร็จ: ' + data.message);
})
.catch(err => {
  console.error('เกิดข้อผิดพลาด:', err);
  alert('เกิดข้อผิดพลาด: ' + err.message);
});
```

> **หมายเหตุ**: แทนที่ `YOUR_GAS_WEB_APP_URL` ด้วย URL ของ Google Apps Script Web App

### วิธีที่ 3: ใช้ Google Sheets Formula

1. เปิด Google Sheets
2. ไปที่ Sheet "Sales"
3. คลิกที่ Column L (Total)
4. เพิ่ม Formula ใน Cell L2:
   ```
   =SUM(C2:K2)
   ```
5. Copy Formula ลงไปทุกแถว:
   - เลือก Cell L2
   - กด Ctrl+C (หรือ Cmd+C)
   - เลือกช่วง L2:L[lastRow]
   - กด Ctrl+V (หรือ Cmd+V)

## 📊 ตรวจสอบผลลัพธ์

### ตรวจสอบใน Google Sheets

1. เปิด Google Sheets
2. ไปที่ Sheet "Sales"
3. ตรวจสอบว่า Column L (Total) ตรงกับผลรวมของ Columns C-K หรือไม่
4. ใช้ Formula `=L2=SUM(C2:K2)` เพื่อตรวจสอบแต่ละแถว

### ตรวจสอบใน Dashboard

1. Refresh Dashboard
2. ตรวจสอบว่า Total ในตารางตรงกับผลรวมของ:
   - เงินสด + โอน + Credit Card + Delivery
3. ตรวจสอบว่า "ยอดขายรวม" ตรงกับผลรวมของ Total ทั้งหมด

## 🔍 ตัวอย่างผลลัพธ์

เมื่อรันฟังก์ชันสำเร็จ จะได้ผลลัพธ์ดังนี้:

```json
{
  "status": "success",
  "message": "Fixed 15 row(s) for all branches",
  "fixedCount": 15,
  "errorCount": 0,
  "fixedRows": [
    {
      "row": 60,
      "branchCode": "SA008",
      "date": "2026-01-15",
      "oldTotal": 10.00,
      "newTotal": 513.46,
      "difference": 503.46
    },
    ...
  ],
  "totalRows": 72
}
```

## ⚠️ หมายเหตุ

1. **Backup ข้อมูล**: แนะนำให้ Backup Sheets ก่อนรันฟังก์ชัน (File > Make a copy)
2. **Deploy Backend**: ต้อง Deploy Google Apps Script ใหม่หลังจากเพิ่มฟังก์ชัน
3. **ตรวจสอบผลลัพธ์**: ตรวจสอบผลลัพธ์ใน Logs หรือ Console หลังรันฟังก์ชัน
4. **Tolerance**: ฟังก์ชันใช้ tolerance 0.01 เพื่อหลีกเลี่ยง floating point errors

## 🚀 Deploy

หลังจากแก้ไข Backend แล้ว:

1. **Deploy Google Apps Script ใหม่**:
   - เปิด Google Apps Script Editor
   - คลิก **Deploy** > **Manage deployments**
   - คลิก **Edit** (ไอคอน ✏️) ข้างๆ Deployment
   - คลิก **New version**
   - คลิก **Deploy**

2. **ทดสอบ**:
   - รันฟังก์ชัน `fixSalesTotalColumn` ใน GAS Editor
   - ตรวจสอบผลลัพธ์ใน Logs
   - ตรวจสอบข้อมูลใน Sheets

3. **Refresh Dashboard**:
   - Refresh หน้าเว็บ
   - ตรวจสอบว่าข้อมูลแสดงถูกต้อง

## 📝 สูตรการคำนวณ

### Total Sales (ยอดขายรวม)
```
Total = Cash + Transfer + Grab + Lineman + Shopee + Robinhood + Credit Card + HalfHalf + Other
```

### Delivery (รวม)
```
Delivery = Grab + Lineman + Shopee + Robinhood
```

## 🔄 การใช้งานซ้ำ

ถ้ามีการบันทึกข้อมูลผิดพลาดอีก สามารถรันฟังก์ชัน `fixSalesTotalColumn` ได้ทุกเมื่อ

**คำแนะนำ**: รันฟังก์ชันนี้เป็นประจำ (เช่น ทุกสัปดาห์) เพื่อให้แน่ใจว่าข้อมูลถูกต้อง
